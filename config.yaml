model:
  path: models/yolov8n.pt
  device: auto
  conf: 0.25
  allowed_classes: []

input:
  source: 0
  is_video: false
  max_fps: null

output:
  export_dir: exports
  enable_export: true

runtime:
  window_name: Object Detection
